paths:
  /repos/{owner}/{repo}/contents/{path}:
    get:
      summary: Get contents of a file
      description: |
        This endpoint retrieves the contents of a file including its SHA. 
        Use this SHA when updating the file to ensure data consistency.
        # ... other specifications
